# Virtual environments
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# Testing & coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Type checking
.mypy_cache/
.ruff_cache/

# Local env
.env

# Database files (never commit a local SQLite dev DB)
*.sqlite
*.sqlite3

# Alembic (if used later) — keep versions/, ignore generated files
alembic/versions/*.py
!alembic/versions/.gitkeep
